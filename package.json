{
  "name": "juggertopdown",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node scripts/deterministic-check.mjs",
    "test:ui": "playwright test",
    "test:ui:headed": "playwright test --headed",
    "screenshots:ui": "playwright test --grep @screenshots",
    "preview": "vite preview",
    "start": "node server/index.js",
    "serve:pvp": "npm run build && node server/index.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "ws": "^8.20.0"
  }
}
